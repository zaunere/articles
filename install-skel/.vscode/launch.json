{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: startdev",
            "request": "launch",
            "command": "npm run startdev",
            "cwd": "${workspaceFolder}"
        }
    ]
}